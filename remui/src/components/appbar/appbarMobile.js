import { AppbarContainer } from "../../styles/appbar";


export default function AppbarMobile({ matches }) {

    return (
        /*
        Appbar continer
        Header
        List
        */
       <AppbarContainer>
           
       </AppbarContainer>
    );
}